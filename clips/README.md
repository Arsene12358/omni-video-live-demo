# clips

Drop demo clips here (`.mp4` / `.mov` / `.webm` / `.mkv`). They're git-ignored; the
backend lists them at `/clips` and serves them at `/clip/<name>`.

Pick clips with a **clear, distinctive opening** — the recall payoff ("what was shown at
the very beginning?") is most convincing when the opening is visually unambiguous.
